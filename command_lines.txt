create table admin(
user_Name varchar(20) NOT NULL,
password varchar(20) NOT NULL);

create table customer(
user_id int not null,
Name varchar(20),
email varchar(30) unique,
password varchar(40) unique,
city varchar(20),
contact_no varchar(10),
PRIMARY KEY (user_id));

create table movie(
movie_id int not null,
movie_name varchar(30),
movie_rating varchar(10),
movie_dimensions varchar(10),
Genre varchar(20),
status varchar(20),
Description varchar(50),
language varchar(20),
imglink varchar(100),
PRIMARY KEY(movie_id));

CREATE TABLE shows (
    timmings TIME,
    show_date DATE,
    theater_id INT NOT NULL,
    movie_id INT NOT NULL,
    screen_no INT,
    screen_dimensions VARCHAR(20),
    no_of_seats INT,
    selected_seats JSON NOT NULL,
    screen_id INT AUTO_INCREMENT PRIMARY KEY,
    FOREIGN KEY (movie_id) REFERENCES movie(movie_id),
    FOREIGN KEY (theater_id) REFERENCES theater(theater_id)
);


create table theater1(
theater_id int,
movie_id int,
foreign key(theater_id) references theater(theater_id),
foreign key(movie_id) references movie(movie_id) 
);

create table bookings(
id int not null auto_increment,
screen_id int not null,
user_id int not null,
no_of_seats int not null,
selected_seats json	not null,
price decimal(10,2) not null,
primary key (id));

create table theater(
theater_id int not null,
theater_name varchar(20),
city varchar(20),
primary key(theater_id));

Insert into movie(movie_id, movie_name, movie_rating, movie_dimensions, Genre, status, Description, langauge, inglink)
Values

Insert into admin(user_Name, password)
value ("Naga", "password");

update movie
set imglink = "https://i.ibb.co/VgM2LLJ/Adipurush.jpg"
where movie_id = 1; 